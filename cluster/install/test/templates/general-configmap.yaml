# 拉取代码仓库的ssh公钥
apiVersion: v1
kind: ConfigMap
metadata:
  name: key
data:
  {{- (.Files.Glob "key/*").AsConfig | nindent 2 }}
---
# 测试用的脚本
apiVersion: v1
kind: ConfigMap
metadata:
  name: shell
  namespace: test
data:
  {{- (.Files.Glob "shell/*").AsConfig | nindent 2 }}